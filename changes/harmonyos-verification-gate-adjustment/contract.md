# Change Contract

Risk: `G2` because this changes a published Domain verification policy.

Acceptance requires the normative rule, workflow, and executable wrapper to agree; deterministic
repository validation to pass; omitted checks to retain explicit `skipped` semantics; and an
independent evaluator to verify that Kernel evidence and authorization controls were not weakened.

The Generator may implement and validate structure but may not issue the independent verdict.
